name: Whitelist Auto Update

on:
  schedule:
    # 每天北京时间凌晨 5:00 自动执行 (对应 UTC 时间 21:00)
    - cron: '0 21 * * *'
  workflow_dispatch:

# 赋予机器人写入和提交代码的权限
permissions:
  contents: write

jobs:
  update-whitelist:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run Auto Update Script
        run: python scripts/update_whitelist.py

      - name: Commit and Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "🛡️ Auto Update: Sync Whitelist version and clean duplicates" || echo "No changes to commit"
          git push
